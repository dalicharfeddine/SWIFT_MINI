//
//  Api.swift
//  CarLovers
//
//  Created by DaliCharf on 20/3/2023.
//

import Alamofire
import AlamofireImage // Add this line

/*class MyService {
    func fetchItems(completion: @escaping ([Item]?) -> Void) {
        Alamofire.request("https://carknights.onrender.com").responseJSON { (response) in
            // Handle response here
            guard let data = response.data else {
                completion(nil)
                return
            }
            let decoder = JSONDecoder()
            do {
                let items = try decoder.decode([Item].self, from: data)
                completion(items)
            } catch {
                print(error)
                completion(nil)
            }
        }
    }
}
 */



